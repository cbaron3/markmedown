// TODO: FOCUS ON COMPLETING FUNCTIONALITY FOR LESSON 1 FIRST
// CREATE LIKE A LIST OF USER STORIES?
// MAKE THEM HIGH PRIORITY WITH SOME SORT OF "LESSON 1 TAG"
// REST OF TASKS CAN BE PUT INTO BACKLOG FOR NOW

// Regex for Markdown syntax highlighting

// Headers
const h1Regex = /(^#{1})(\s)(.*)/;
const h2Regex = /(^#{2})(\s)(.*)/;
const h3Regex = /(^#{3})(\s)(.*)/;
const h4Regex = /(^#{4})(\s)(.*)/;
const h5Regex = /(^#{5})(\s)(.*)/;
const h6Regex = /(^#{6})(\s)(.*)/;
